2. git clone https://github.com/aosuri2/qtm350-quiz01 
3. cd qtm350-quiz01 
4. touch commands.txt 
5. git checkout -b feature-update 
6. mkdir reports 
7. touch reports/summary.md 
8. echo "This document provides a summary of the project updates" > summary.md
9. git add reports 
10. git commit -m "Add reports directory" 
11. touch reports/file{1..3}.txt 
12. mv data/raw-data.csv data/input-data.csv 
13. mkdir scripts/backup && cp scripts/*.py scripts/backup/ 
14. rm docs/documentation.md 
15. git add . && git commit -m "Update project files" 
16. touch .gitignore 
17. echo "temp/dataset0?.csv" > .gitignore 
18. cat .gitignore 
19. git add .gitignore && git commit -m "Add .gitignore file" 
20. git checkout main && git merge feature-update
21. git add . && git commit -m "This is my final commit"
22. git push

For the bonus questions:
1. git push -u origin feature-update
#go to compare and list mine as the main and dr. freire's as the base to compare

base repository: danilofreire/qtm350-quiz01
head repository: aosuri2/qtm350-quiz01
"create pull request"

2. git checkout feature-update
git rebase -i HEAD~2
#change pick fbe051d Add .gitignore file to squash fbe051d Add .gitignore file
#click esc and then type :wqa
#change the line to update project files and add .gitignore
#click esc and then type :wqa
git push

